import{useState}from'react';import{f1}from'../lib/openf1';
export function AiChat({context}:{context:any}){const[messages,setMessages]=useState<any[]>([]),[text,setText]=useState(''),[loading,setLoading]=useState(false);async function send(){if(!text.trim())return;const next=[...messages,{role:'user',content:text}];setMessages(next);setText('');setLoading(true);const r:any=await f1.chat({context,messages:next});setMessages([...next,{role:'assistant',content:r.answer}]);setLoading(false)}return <div className="card space-y-3"><h2 className="text-2xl font-bold">ИИ-чат GigaChat</h2><p className="text-sm text-slate-400">Спросите простым языком: кто выиграл, что такое пит-стоп, какие были возможные проблемные моменты.</p><div className="max-h-80 space-y-2 overflow-auto rounded-xl bg-black/30 p-3">{messages.map((m,i)=><div key={i} className={m.role==='user'?'text-right':''}><span className="inline-block rounded-xl bg-white/10 p-2">{m.content}</span></div>)}{loading&&<p className="text-slate-400">GigaChat думает…</p>}</div><div className="flex gap-2"><input className="input flex-1" value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Объясни гонку простыми словами"/><button className="btn" onClick={send}>Отправить</button></div></div>}
